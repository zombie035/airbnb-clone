<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results - LandBnb</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        crimson: '#990000',
                        'crimson-light': '#fff5f5',
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-gray-50 min-h-screen">
    <%- include('../partials/nav') %>
    
    <main class="container mx-auto px-4 py-8">
        <div class="mb-8">
            <h1 class="text-3xl font-bold text-gray-900 mb-2">
                Search Results for "<span class="text-crimson"><%= searchQuery %></span>"
            </h1>
            <p class="text-gray-600">
                Found <span class="font-semibold text-crimson"><%= homes.length %></span> result(s)
            </p>
        </div>
        
        <% if (homes.length > 0) { %>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <% homes.forEach(home => { %>
                    <div class="bg-white rounded-2xl shadow-sm overflow-hidden hover:shadow-lg transition-shadow duration-300 border border-gray-100">
                        <div class="h-48 overflow-hidden">
                            <img src="<%= home.photo || 'https://placehold.co/600x400/f3f4f6/990000?text=Home+Image&font=montserrat' %>" 
                                 alt="<%= home.houseName %>" 
                                 class="w-full h-full object-cover hover:scale-105 transition-transform duration-500"
                                 onerror="this.src='https://placehold.co/600x400/f3f4f6/990000?text=Image+Not+Found&font=montserrat'">
                        </div>
                        <div class="p-5">
                            <div class="flex justify-between items-start mb-2">
                                <h3 class="text-lg font-bold text-gray-900 truncate"><%= home.houseName %></h3>
                                <div class="flex items-center">
                                    <i class="fas fa-star text-yellow-400 mr-1"></i>
                                    <span class="font-semibold"><%= home.rating %></span>
                                </div>
                            </div>
                            <p class="text-gray-600 text-sm mb-3 flex items-center">
                                <i class="fas fa-map-marker-alt text-crimson mr-2"></i>
                                <%= home.location %>
                            </p>
                            <div class="flex justify-between items-center mb-4">
                                <span class="text-crimson font-bold text-xl">₹<%= home.price %><span class="text-sm font-normal text-gray-600">/night</span></span>
                            </div>
                            <% if (home.description) { %>
                                <p class="text-gray-700 text-sm mb-4 line-clamp-2"><%= home.description.substring(0, 100) %>...</p>
                            <% } %>
                            <div class="flex gap-2">
                                <a href="/homes/<%= home._id %>" 
                                   class="flex-1 text-center bg-crimson text-white py-2 rounded-lg font-semibold hover:bg-[#880000] transition">
                                    View Details
                                </a>
                                <button class="w-10 h-10 flex items-center justify-center border border-gray-300 rounded-lg hover:border-crimson hover:text-crimson transition">
                                    <i class="far fa-heart"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="text-center py-16">
                <div class="w-24 h-24 mx-auto mb-6 text-gray-300">
                    <i class="fas fa-search text-6xl"></i>
                </div>
                <h2 class="text-2xl font-bold text-gray-700 mb-4">No homes found</h2>
                <p class="text-gray-600 mb-8 max-w-md mx-auto">
                    We couldn't find any homes matching "<span class="font-semibold text-crimson"><%= searchQuery %></span>". Try searching with different keywords.
                </p>
                <div class="flex flex-col sm:flex-row gap-4 justify-center">
                    <a href="/homes" class="px-6 py-3 bg-crimson text-white rounded-lg font-semibold hover:bg-[#880000] transition">
                        <i class="fas fa-home mr-2"></i>Browse All Homes
                    </a>
                    <a href="/" class="px-6 py-3 border-2 border-gray-300 text-gray-700 rounded-lg font-semibold hover:border-crimson hover:text-crimson transition">
                        <i class="fas fa-arrow-left mr-2"></i>Go Home
                    </a>
                </div>
            </div>
        <% } %>
        
        <% if (homes.length > 0) { %>
        <div class="mt-12 pt-8 border-t border-gray-200">
            <h3 class="text-lg font-bold text-gray-800 mb-4">Search Tips:</h3>
            <div class="flex flex-wrap gap-3">
                <a href="/search?q=villa" class="px-4 py-2 bg-gray-100 text-gray-700 rounded-full text-sm hover:bg-crimson hover:text-white transition">
                    <i class="fas fa-building mr-1"></i>Villa
                </a>
                <a href="/search?q=beach" class="px-4 py-2 bg-gray-100 text-gray-700 rounded-full text-sm hover:bg-crimson hover:text-white transition">
                    <i class="fas fa-umbrella-beach mr-1"></i>Beach
                </a>
                <a href="/search?q=mountain" class="px-4 py-2 bg-gray-100 text-gray-700 rounded-full text-sm hover:bg-crimson hover:text-white transition">
                    <i class="fas fa-mountain mr-1"></i>Mountain
                </a>
                <a href="/search?q=luxury" class="px-4 py-2 bg-gray-100 text-gray-700 rounded-full text-sm hover:bg-crimson hover:text-white transition">
                    <i class="fas fa-crown mr-1"></i>Luxury
                </a>
                <a href="/search?q=budget" class="px-4 py-2 bg-gray-100 text-gray-700 rounded-full text-sm hover:bg-crimson hover:text-white transition">
                    <i class="fas fa-wallet mr-1"></i>Budget
                </a>
            </div>
        </div>
        <% } %>
    </main>
    
 
    
    <script>
        // Add to favorites functionality
        document.addEventListener('DOMContentLoaded', function() {
            const favoriteButtons = document.querySelectorAll('.fa-heart');
            
            favoriteButtons.forEach(button => {
                button.addEventListener('click', function() {
                    this.classList.toggle('far');
                    this.classList.toggle('fas');
                    this.classList.toggle('text-red-500');
                    
                    if (this.classList.contains('fas')) {
                        // Add to favorites
                        console.log('Added to favorites');
                    } else {
                        // Remove from favorites
                        console.log('Removed from favorites');
                    }
                });
            });
            
            // Highlight current search in navbar if it exists
            const searchInput = document.querySelector('input[name="q"]');
            if (searchInput && '<%= searchQuery %>') {
                searchInput.value = '<%= searchQuery %>';
            }
        });
    </script>
</body>
</html>